problems:
  - id: singleton_bound_from_sum_form
    tags: [coding_theory, singleton_bound]
    hint: |
      This is linear arithmetic on Nat.
      Preferred proof:
        omega
      If omega fails, try:
        nlinarith
    formal_statement: |
      import Mathlib

      /-
      Core arithmetic form of Singleton bound:
      if k + d ≤ n + 1 then d ≤ n - k + 1.
      -/
      theorem singleton_bound_from_sum_form (n k d : Nat)
          (h : k + d ≤ n + 1) : d ≤ n - k + 1 := by
        sorry
